{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna run tsc && lerna publish",
    "build": "lerna bootstrap && lerna run tsc",
    "test": "lerna run test"
  },
  "devDependencies": {
    "@shelf/jest-mongodb": "1.2.3",
    "@types/jest": "27.5.1",
    "@types/node": "13.13.15",
    "jest": "28.1.0",
    "lerna": "3.22.1",
    "ts-jest": "28.0.2",
    "typescript": "3.8.3"
  }
}
